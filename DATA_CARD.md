# Data Card

Fill before any reported run.

- Dataset source:
- License:
- Tokenizer:
- Tokenizer version/hash:
- Train/val split:
- Deduplication:
- Contamination checks:
- Number of train tokens:
- Number of validation tokens:
- File fingerprints:

Required token file format:

- `.bin` raw int32 token ids plus `.json` sidecar, or `.pt` 1-D integer tensor.
