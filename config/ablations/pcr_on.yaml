# Ablation: PCR ON (predictive cascade refinement)
# Refines predictions in orthogonal subspaces to bypass information bottleneck
model:
  use_jawp: true
  use_pcr: true
  pcr_n_levels: 3
  pcr_level_dims: null
  pcr_warmup_steps: 1000
